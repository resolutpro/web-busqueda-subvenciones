## Packages
framer-motion | Page transitions and detailed animations
recharts | Dashboard charts for grant analytics and match scores
date-fns | Formatting dates for grant timelines
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
Authentication uses Replit Auth (blueprint:javascript_log_in_with_replit).
Onboarding flow required if `/api/companies/me` returns 404.
Dashboard requires complex data visualization for match scores.
